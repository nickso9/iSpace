
<p class="mt-3 mb-2">Search users to add:</p>


<nav class="navbar navbar-light">
    
  <form class="form-inline form-friend-search mx-auto">
     <div class="d-inline-block">
        <input class="form-control" type="search"  aria-label="Search">
    </div>
    <div class="d-inline-block">
        <button class="btn btn-outline-dark search-btn" type="submit">Search</button>
    </div> 
  </form>
</nav>



<div class="friend-div"></div>


{{#each user.pendingFriends}}
   <div class="card bg-light" style="border: 1px solid rgb(255, 145, 0)">
                        <div class="row m-0">
                        <div class="">
                            <img src="{{this.image}}" class="card-img-friend" alt="...">
                        </div>
                        <div class="card-body border-0">
                            <h5 class="card-title">{{this.username}}</h5>
                            <p class="card-text">{{this.location}}</p>
                        </div>
                        <div class="d-flex" style="position: relative">
  
                            <button type="submit" class="btn friend-add-btn align-self-end m-3" id="${response.userId}">Pending friend</button>
                            {{!-- <span class="error-friend" style="font-size: 10px; color: red; display: none; position: absolute; bottom: 0">Friend requests already pending.</span> 
                            <span class="success-friend" style="font-size: 10px; color: green; display: none; position: absolute; bottom: 0">Requested friendship.</span>    --}}
                        </div>
                        
                        </div>
         </div>`

{{/each}}