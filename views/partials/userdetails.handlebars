

<div class="container user-profile bg-light d-flex">
    <div class="d-inline mr-4">
        <img src='{{user.profile.image}}' class="d-inline mt-4 profile-image" id="check-image">
    </div>

    <div class="d-inline mt-2" class="user-details" id='{{user.id}}'>
        <p class="mt-2 d-block"><span class="profile-text">Username:</span></p> <span id="check-user" class='profile-bio-text'>{{user.profile.username}}</span>
        <p class="mt-2 d-block"><span class="profile-text">Headline:</span> </p><span class='profile-bio-text'>{{user.profile.headline}}</span>
        <p class="mt-2 d-block"><span class="profile-text">Location:</span></p> <span class='profile-bio-text' id="check-location">{{user.profile.location}}</span>
        <p class="mt-2 d-block"><span class="profile-text">Bio:</span></p><span class='profile-bio-text'>{{user.profile.bio}}</span>

    </div>
    
</div>






