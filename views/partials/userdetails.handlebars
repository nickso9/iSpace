

<div class="container user-profile bg-light">
    <div class="d-inline">
        <p><span class="profile-text">Profile Picture:</span></p>
        <img src='{{user.profileimage}}' class="d-inline mt-1">
    </div>

    <div class="d-inline mt-2">
        <p class="mt-2"><span class="profile-text">Email:</span> {{user.email}}</p>
        <p class="mt-2"><span class="profile-text">Username:</span> {{user.profileusername}}</p>
        <p class="mt-2"><span class="profile-text">Headline:</span> {{user.profileheadline}}</p>
        <p class="mt-2"><span class="profile-text">Birthday:</span> {{user.profilebirthday}}</p>
        <p class="mt-2"><span class="profile-text">Location:</span> {{user.profilelocation}}</p>
        <p class="mt-2"><span class="profile-text">Profile:</span> {{user.profilebio}}</p>

    </div>
    
</div>






